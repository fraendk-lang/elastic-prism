# Optional: copy to .env.local for local overrides (not required for core visuals).

# Vercel only: set in Project → Settings → Environment Variables.
# If set, the whole site asks for a preview password before loading.
# Remove or leave empty when you launch publicly.
# SITE_PASSWORD=your-secret-preview-password
